FROM alpine
ENV MUNKI_REPO_PATH=/data
ENV MUNKI_HOSTNAME=0.0.0.0
ENV MUNKI_PORT=443
ENV MUNKI_TLS_EMAIL=""
ENV MUNKI_AUTH_USER="munki"
ENV MUNKI_AUTH_PASSWORD="secret"


RUN apk --no-cache add git ca-certificates 

COPY root /

VOLUME /data
EXPOSE 80 443 2015

CMD ["/ape"]
